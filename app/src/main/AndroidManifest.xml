<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.servets.kof">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_cofee"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondA"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddToDB"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ListZamow" />
        <activity android:name=".DopFunk" />
        <activity android:name=".AllDataForm" />
        <activity
            android:name=".Settings"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MonthData" />
        <activity android:name=".Zapros"
            android:parentActivityName=".MonthData"/>
    </application>

</manifest>